# 版本紀錄

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/) 的分類方式，產品版本採 [Semantic Versioning](https://semver.org/lang/zh-TW/)。Sites 平台版本與產品語意版本分開記錄。

## [Unreleased]

### Fixed

- 唯讀分享地圖的工具列改用原生 `disabled` 表達停用狀態。先前只靠 `pointer-events:none` 擋滑鼠，鍵盤使用者仍可 Tab 進工具列並按 Enter 新增節點；`<nav>` 上的 `aria-disabled` 則因 `role=navigation` 不支援而被輔助科技忽略。
- 「更多工具」面板中會改動地圖的項目（複製分支、移除節點、分支範本、匯入內容、知識匯入）在唯讀模式一併停用，使用者偏好與檢視切換仍可使用。
- `addNode()` 與 `removeSelectedNode()` 補上 `canEdit` 防護，涵蓋節點卡片、靈感收件匣與鍵盤等其他呼叫路徑。
- 共享地圖頁面不再於 `try/catch` 內建構 JSX；React 延後渲染會讓元件自身的錯誤逃出該 catch，卻被誤報成資料庫連線失敗。
- 節點功能操作列改為平時隱藏，只在滑過、鍵盤聚焦或選取節點時顯示，降低畫布上的圖示干擾。
- 操作列移至節點卡片外側，並還原標題與說明可使用的完整內距，避免功能按鈕遮擋文字內容。
- 深色中心節點的浮動操作列改用暖白背景與深色文字，桌面與手機皆維持清楚對比。

### Verified

- eslint 0 error 0 warning；76 項自動測試通過，並新增 7 項唯讀權限防退化斷言（已確認能抓到修正前的程式碼）。
- production build 成功；正式網站桌面與 390×844 手機尺寸均確認操作列位於文字區域外，未產生重疊。
- 已驗證未選取節點隱藏操作列、選取節點顯示完整功能，瀏覽器主控台無警告或錯誤。
- Sites 第 40 版已發佈至正式網站。

### Planned

- P2-02 分享到期、撤銷與重新產生策略，以及 P2-03 節點留言與討論。

## [0.29.0] - 2026-08-07

### Changed

- 左側三條線檢視按鈕改為依序循環「心智圖 → 樹狀 → 大綱 → 心智圖」，三種檢視都可從工具列直接到達。
- 按鈕的無障礙名稱與懸浮提示會依目前檢視顯示下一個模式。

### Verified

- 伺服器渲染與原始碼回歸測試通過，並加入三模式循環順序的防退化斷言。

## [0.28.2] - 2026-08-07

### Fixed

- 修正 Safari 將「更多工具」面板裁切在可捲動左側工具列內，造成點擊三點按鈕後看似沒有反應的問題。
- 面板改由工具列的外層工作區呈現，保留既有位置、背景關閉、鍵盤名稱與全部工具。

### Verified

- 伺服器渲染與原始碼回歸測試通過，並新增面板不得位於捲動工具列內的防退化斷言。

## [0.28.1] - 2026-08-07

### Changed

- 重新設計左側工具列，只保留新增、AI 自動產圖、靈感收件匣、復原／重做、大綱與更多工具等高頻入口。
- 將移除節點、複製分支、分支範本、內容匯入、知識匯入、使用者偏好與重設範例整合至「更多工具」面板，降低主列長度與認知負擔。
- 工具列改為上下分區，復原／重做合併為緊湊控制；桌面與手機的短視窗都能直接操作底部工具，不需捲到隱藏位置。

### Verified

- 76 項自動測試全數通過，既有節點、AI、匯入、歷史、保存與分享功能未受影響。
- Sites 第 36 版已保存；共享／公開網站的正式部署仍待明確核准。

## [0.28.0] - 2026-07-27

### Added

- 新增 P2-01 分享權限：擁有者可設定唯讀、可留言或可編輯，分享預設關閉。
- 新增公開 `/s/:token` 訪客頁面、角色提示，以及分享啟用、停用、複製和重新產生連結介面。
- 新增 `share_links` D1 資料表與 migration；權限在讀寫 API 伺服器端強制執行。

### Changed

- 本機地圖的雲端建立改為需要 ChatGPT 身分，並先建立私人個人地圖，再由擁有者主動開啟分享。

### Verified

- 76 項自動測試通過，包含角色驗證、非法設定拒絕、私人擁有權、403 寫入防護與介面整合。
- production build 成功，包含分享管理 API 與公開 token 路由。

## [0.27.0] - 2026-07-26

### Added

- 新增 PDF、公開 HTTPS 網站與影音逐字稿知識匯入，共用 AI 心智圖草稿預覽與確認建立流程。
- PDF 支援 8MB 以下文件；影音支援貼上內容或上傳 TXT、Markdown、SRT、VTT。
- 網站匯入加入公開網址、HTML、大小、逾時與重新導向限制，降低伺服器端請求風險。

### Verified

- 74 項自動測試通過，包含來源驗證、私人網址拒絕、網站正文清理與介面整合。
- 隔離暫存副本的 production build 成功。

## [0.26.0] - 2026-07-26

### Added

- 新增 AI 自動產圖：輸入主題後可選精簡、標準或深入詳細程度，產生最多 12、24 或 40 個節點。
- 新增完整草稿預覽，顯示文件標題、生成摘要與節點階層；確認後才取代目前地圖。
- 新增獨立 `/api/generate-map` 伺服器端點與結構化輸出，驗證唯一中心、父節點順序、識別字、內容長度與節點上限。

### Verified

- 71 項自動測試通過，包含產圖請求、提示約束、非法階層拒絕、節點上限與布局轉換。
- 隔離暫存副本的 production build 成功。

## [0.25.0] - 2026-07-26

### Added

- 新增範本商城，內含 11 個精選範本與目標、專案、教育、研究、行銷、敏捷、AI、生活等分類。
- 支援關鍵字／標籤搜尋、分類篩選、範本作者與節點數資訊，以及完整結構預覽。
- 可將預覽範本一鍵套用到目前節點，沿用自動布局與單步復原。

### Verified

- 67 項自動測試通過，包含商城分類、標籤／描述搜尋、無結果與範本資料完整性。
- 隔離暫存副本的 production build 成功。

## [0.24.0] - 2026-07-26

### Added

- 節點右鍵選單新增 AI Assistant，可直接執行展開想法、找風險、SWOT、OKR、Brainstorm、簡化內容、翻譯成英文與摘要。
- 節點操作列加入「✦」入口，讓觸控與鍵盤使用者不必依賴右鍵。
- AI 指令結果沿用既有預覽：分析結果可逐項或整組加入，簡化／翻譯／摘要可確認後寫回節點說明。

### Verified

- 66 項自動測試通過，包含八種指令完整性、模式映射與結果類型。
- 隔離暫存副本的 production build 成功。

## [0.23.0] - 2026-07-26

### Added

- 可複製選取節點及完整子樹，新節點使用全新 ID 並保留正確父子關係；一次操作只建立一筆可復原歷史。
- 新增 SMART 目標、決策分析與每週回顧三個內建分支範本，可套用在任意節點下。
- 支援 JSON／Markdown 檔案或貼上匯入；匯入前顯示格式、標題與節點數，格式錯誤會指出行號或節點序號。
- 新增可完整往返的 JSON 匯出。
- 裝置端保存預設檢視、AI 面板狀態、10～200% 縮放與減少動態偏好，並可一鍵重設。

### Verified

- 65 項自動測試全數通過，涵蓋子樹 ID 重建、範本父子關係、JSON／Markdown 錯誤定位、既有 Markdown 匯出往返與偏好驗證。
- 隔離暫存副本的正式建置成功。

## [0.22.0] - 2026-07-26

### Added

- 新增 `npm run benchmark`，固定量測 100／300／500 節點的拖曳狀態更新、搜尋、文字輸入、畫面衍生資料與樹狀布局，超出 p95 門檻時以失敗結束。
- 新增僅限本機開發的 100／300／500 節點實際畫面基準入口；正式環境回傳 404。

### Changed

- 搜尋比對改用延後值，避免大型地圖的非緊急醒目標示搶走輸入回應。
- 大綱階層、子節點數量、父節點與同層節點改為共用 Map 索引，不再對每一列重複掃描完整節點陣列。

### Verified

- 500 節點基準 p95：拖曳狀態 0.013ms、搜尋 0.036ms、文字輸入 0.004ms、畫面衍生資料 1.624ms、樹狀布局 0.371ms；全部低於 16／50／300ms 預算。
- 60 項自動測試全數通過，包含大型地圖索引、正規化搜尋與正式環境隱藏基準入口的迴歸測試。

## [0.21.0] - 2026-07-26

### Added

- 節點可用鍵盤移動：以 Tab 選取節點後，方向鍵每次移動 8px，按住 Shift 移動 32px；連續按壓只建立一筆復原紀錄，樹狀模式只改動檢視偏移量、不動自由畫布座標。
- 心智圖與樹狀節點可用鍵盤聚焦；聚焦到畫面外的節點時，畫布會自動平移到不被命令列或縮放控制遮住的位置。

### Changed

- 觸控裝置上所有主要操作的可點擊範圍至少 44×44px；節點的編輯、智慧整理、移植與新增改為浮出於選取卡片下方的操作列，不再擠在卡片內。
- 焦點指示改為單一高對比外框，並涵蓋連結、下拉、`summary` 與畫布節點；深色底控制項改用暖色環，節點選取外框同步提高對比。
- 外框最小高度由 680px／760px 降為 480px 並改用 `dvh`，讓 200% 縮放後的短視窗不再裁切面板；桌面工具列在高度不足時可捲動。

### Verified

- 焦點指示對比實測：紙張底 5.92:1、暖白底 6.40:1，深色底暖色環對 ink 11.80:1；節點選取外框由 1.48:1 提高至 4.22:1（皆為 alpha 合成後的實際顏色）。
- `npm run lint`、正式建置、`git diff --check` 與 58 項自動測試全數通過。
- 尚未在實體觸控裝置與螢幕閱讀器上完成人工驗收，僅以自動化斷言把關。

## [0.20.0] - 2026-07-25

### Added

- 心智圖與樹狀畫布支援滑鼠滾輪／觸控板縮放，以及觸控螢幕雙指縮放；縮放以游標或雙指中心為錨點，並以動畫幀合併高頻更新。

### Verified

- 滑鼠滾輪縮放從 100% 調整至 127% 時，游標錨點位移小於 0.001 像素，主控台無警告或錯誤。
- `npm run lint`、正式建置、`git diff --check` 與 56 項自動測試全數通過。

## [0.19.1] - 2026-07-25

### Fixed

- AI 擴寫草稿面板新增明確且穩定的垂直捲動區與可見捲動軸；桌面與手機都能捲到最後一組建議並完成點選。
- 修正 AI 面板內容因 flex 最小高度與底部空間不足，導致第五組建議被裁切或難以操作的問題。

### Verified

- 真實 AI 產生 6 組擴寫草稿；桌面 1280×720 與手機 390×844 均可捲到最後一組，第五組完整可見並成功加入。
- `npm run lint`、正式建置、`git diff --check` 與 55 項自動測試全數通過；瀏覽器主控台無警告或錯誤。

## [0.19.0] - 2026-07-25

### Added

- 新增大、中、小階層卡片：中心主題、第一層主要分支與第二層以後細節節點使用一致的畫面、碰撞、連線及匯出尺寸規格。
- 選取具有子節點的節點時顯示獨立「收合 N／展開 N」控制，並以 `aria-expanded` 說明目前狀態。
- 分支收合狀態依本機或雲端地圖 ID 版本化保存在目前裝置，損壞資料與已刪除節點 ID 會安全忽略或清理。

### Changed

- 「適合畫面」改以實際可用畫布與中心主題四周的不對稱範圍計算縮放、平移；扣除命令列、縮放控制與手機 AI 面板安全區域。
- 超大型地圖的總覽縮放下限由 50% 降為 10%，比例向下取整，降低邊界裁切。
- 全圖智慧整理依子樹葉節點數分配扇區，父節點置於後代扇區中央，各層半徑依卡片尺寸與節點密度向外展開；指定分支整理保持根節點與外部節點不動。
- 樹狀模式的適合畫面維持樹根靠下、主幹水平置中的閱讀語意，同時完整顯示可見樹冠。

### Fixed

- 修正不對稱心智圖按下「適合畫面」後中心主題偏離可用畫布中央的問題。
- 收合父節點時若焦點位於其後代，焦點會回到父節點；隱藏後代及相關連線，展開後完整恢復。

### Verified

- 純函式涵蓋不對稱置中、100 節點避讓、三種階層尺寸、局部分支錨定、多層收合與版本化檢視狀態。
- 桌面 1280×720、手機 390×844 與樹狀模式完成智慧整理、適合畫面及收合重整驗收；主控台無警告或錯誤。
- `npm run lint`、正式建置、`git diff --check` 與 55 項自動測試全數通過。

## [0.18.1] - 2026-07-25

### Fixed

- 逐一加入 AI 擴寫草稿後，不再把焦點跳到新建立的子節點；原節點與整組草稿會留在畫面，可繼續選擇第二、第三項。
- 已加入的建議會顯示勾選完成狀態並停用重複加入，其餘建議與「全部加入圖中」數量同步保留。

### Verified

- 真實 AI 生成 6 項擴寫後，連續加入前兩項；兩個節點均建立成功、焦點保持原節點，剩餘數量由 6 → 5 → 4。
- 正式建置、`git diff --check` 與 50 項自動測試全數通過；瀏覽器主控台無警告或錯誤。

## [0.18.0] - 2026-07-25

### Added

- 樹狀檢視新增木質漸層、枝條輪廓、樹皮紋理與分叉木節，使主幹、粗枝與末梢更接近自然樹形。
- 新增連線交叉與卡片邊界錨點測試，確保智慧整理結果維持清楚、可讀。

### Changed

- 全圖「智慧整理」改為環狀扇區布局，將分支與節點均勻分散至中心四周，不再集中於左右兩側。
- 心智圖連線改從節點卡片邊界出入，移除交替彎折，降低線條交錯、中心堆疊與穿越節點的情況。
- 自動適合畫面會為上方命令列保留空間，避免最上層節點被模式切換與搜尋控制遮住。

### Verified

- 100 節點自動布局無節點重疊、四向均勻分布，且父子連線零嚴格交叉。
- 桌面 1280×720 實際驗證智慧整理、復原入口、心智圖／樹狀切換與自然枝幹渲染。
- `npm run lint`、正式建置、`git diff --check` 與 50 項自動測試全數通過。

## [0.17.0] - 2026-07-25

### Added

- 新增「靈感收件匣」抽屜，可將多行筆記、條列內容或快速輸入拆成尚未分類的種子。
- 種子支援標題／補充說明、來源標示、單筆移除、整批清空，以及選擇任一心智圖節點後「種到分支」。
- 新增收件匣 AI 腦力激盪；AI 依目前節點與使用者輸入方向產生 3–6 顆種子，不直接改動心智圖。
- 收件匣依本機地圖或雲端地圖 ID 使用版本化裝置儲存，重新整理後仍可繼續分類。

### Changed

- 多行輸入會清除常見項目符號、合併空白、辨識「標題｜補充」格式並過濾重複想法。
- 工具列新增收件匣入口與待分類數量；桌面採浮動抽屜，手機採保留 10px 邊界的可捲動面板。
- 將種子加入分支時沿用既有節點新增、復原、分支展開與自動保存流程。

### Verified

- `npm run lint`、正式建置、`git diff --check` 與 49 項自動測試全數通過。
- 實際完成「貼上三行（含一筆重複）→ 產生兩顆種子 → 種到身心健康分支 → 可復原」，並確認重新整理後種子仍存在。
- 真實 AI 成功產生六顆不重複種子；手機 390×844 面板完整位於視窗內且無水平溢位。

## [0.16.0] - 2026-07-25

### Added

- 樹狀檢視支援拖曳節點微調樹冠；每個節點使用獨立的檢視偏移量，切回心智圖時不改變原本自由畫布座標。
- 樹狀節點移動時，主幹與分枝會依新的顯示座標同步重繪；「智慧整理」可清除手動偏移並恢復自動樹冠。

### Changed

- 心智圖與樹狀拖曳改為以 `requestAnimationFrame` 合併高頻指標更新，每個畫面更新週期最多寫入一次 React 狀態。
- 拖曳期間停用節點 `left/top` 座標動畫，放開滑鼠後不再出現卡片追趕游標的延遲。
- 加入 3px 移動門檻、`pointerdown` 預設行為阻擋與拖曳中全畫布文字選取防護；雙擊編輯與單一節點選取仍維持正常。
- 樹狀節點游標與操作提示改為可拖曳狀態，明確說明可微調樹冠。

### Verified

- `npm run lint`、正式建置、`git diff --check` 與 46 項自動測試全數通過。
- 桌面 1280×720 實際驗證心智圖節點立即移動、單步復原、樹狀節點與枝條同步、模式座標隔離、偏移保留及智慧整理重設。
- 拖曳後只保留一個選取節點，瀏覽器文字選取為空；雙擊編輯正常，主控台無警告或錯誤。

## [0.15.0] - 2026-07-25

### Added

- AI 思考助手新增「自動擴寫」，依目前節點、上層脈絡與既有子節點生成 3–6 個互補草稿。
- 擴寫草稿可逐一加入或一次全部加入；整組加入會自動整理畫布，且只建立一筆可復原歷史。
- 新增「概念解讀」，產生白話定義、核心重點、圖中節點關聯與延伸問題。
- 概念解讀的精簡摘要可直接設為節點說明，並支援復原。

### Changed

- AI 端點依擴寫與解讀任務使用不同嚴格 JSON Schema，伺服器會驗證輸出長度、節點 ID 與必要欄位。
- AI 提示納入祖先節點及既有子節點，擴寫會明確避免同義重複；解讀資訊不足時要求以保留語氣表達。
- AI 面板維持原有視覺系統，桌面使用雙模式控制，手機沿用可收合 bottom sheet。

### Verified

- `npm run lint`、正式建置與 45 項自動測試全數通過。
- 本機以真實 Responses API 完成擴寫與解讀請求；實際驗證整組加入 5 個節點、單步復原、摘要寫入與復原。
- 桌面與手機 390×844 介面無水平溢位，瀏覽器主控台無相關警告或錯誤。
- 本版尚未發布到正式站，待確認後再同步 GitHub 主分支與 Sites 正式版本。

## [0.14.0] - 2026-07-25

### Added

- 新增「移植分支」操作，可將整個分支改掛到另一個父節點；選擇面板會排除自己與所有後代，避免形成循環。
- 大綱模式新增縮排與凸排，可直接調整分支層級，並保留同層順序與完整後代。

### Changed

- 移植、縮排與凸排完成後會自動整理節點位置、展開新的父節點並保持移動節點為目前選取項目。
- 每次結構變更只建立一筆歷史紀錄，可完整復原與重做。
- 手機版大綱操作改為固定寬度的 2×2 結構按鈕，避免窄螢幕水平溢位。

### Verified

- `npm run lint`、正式建置與 42 項自動測試全數通過。
- 桌面 1280×720 與手機 390×844 實際完成跨分支移植、循環目標排除、復原／重做、縮排與凸排；手機版無水平溢位，瀏覽器主控台無警告或錯誤。
- 本版尚未發布到正式站，待確認後再同步 GitHub 主分支與 Sites 正式版本。

## [0.13.0] - 2026-07-24

### Added

- 新增「樹狀」檢視，將中心主題置於底部，依階層向上生成主幹、分枝與葉冠。
- 三模式切換列提供心智圖、樹狀與大綱；手機版同樣保留完整切換、搜尋與整理入口。
- 樹狀模式的 PNG／PDF 匯出會使用目前樹形布局與由粗至細的分枝。

### Changed

- 樹狀布局由原始節點資料即時計算，不覆寫自由畫布座標，也不額外建立復原紀錄。
- 樹狀節點不可任意拖離枝條，但保留選取、編輯、新增、收合、搜尋、縮放、平移與智慧整理。
- 樹狀連接線改為共用主幹與分段剝離的彩色枝條；外側粗枝較早分出、內側枝條沿主幹向上延伸，第一層節點同步高低錯落，避免線條穿越相鄰卡片。

### Verified

- 正式建置成功，40 項自動測試全數通過；涵蓋樹狀布局向上生長、無重疊、資料不變、100 節點決定性、共用主幹與分段分枝。
- 桌面 1536×1024 與手機 390×844 實際驗證完成；搜尋、分枝收合／展開與就地編輯正常，瀏覽器主控台無錯誤。
- 與使用者提供的樹形連接線附件並排比較後，完成主幹粗細、分枝高度、垂直節奏與畫面留白修正。

## [0.12.0] - 2026-07-24

### Added

- `Cmd/Ctrl+Z` 鍵盤復原，以及 `Cmd/Ctrl+Shift+Z`、`Ctrl+Y` 鍵盤重做；文字輸入框保留原生文字歷史。
- 雲端地圖的裝置端離線草稿保護，以地圖 ID 隔離未同步內容，重開頁面可自動復原。
- 同步失敗與離線狀態提示，提供手動重新同步，恢復連線後也會自動重試。

### Changed

- 雲端保存改為先寫入安全草稿再送出；同步期間若持續編輯，會在目前請求完成後接續保存最新版本。
- 成功同步或選擇載入衝突最新版後會清除安全草稿，避免舊內容重複覆蓋。

### Verified

- 正式建置成功，36 項自動測試全數通過。
- 實際操作新增節點、鍵盤復原與鍵盤重做均成功；桌面與 390×844 手機版無水平溢位，瀏覽器主控台無錯誤。
- 發布至 Sites 平台版本 21，並以 `v0.12.0` 標籤同步 GitHub 主分支。

## [0.11.1] - 2026-07-24

### Changed

- 節點的編輯、收合、智慧整理與新增操作改為卡片內的緊湊操作列，不再突出節點邊界或擠壓標題。
- 桌面圓形按鈕縮為 24px；手機維持 28px 視覺尺寸，並在滑入、選取或鍵盤聚焦時提高對比。

### Verified

- 正式建置、lint 與 33 項自動測試全數通過。
- 桌面 1280×720 與手機 390×844 視覺驗證通過；取樣節點的操作列皆完整位於節點方塊內。
- 編輯與新增操作已在本機預覽實際驗證，瀏覽器主控台無錯誤。

## [0.11.0] - 2026-07-24

### Added

- 「智慧整理」可一鍵重排整張心智圖；節點快捷操作可只整理指定分支。
- 雙向整潔樹布局會固定中心或分支根節點、延續既有左右方向，並避開未整理的節點。
- `/maps` 個人地圖工作區，可建立、重新命名、複製、搜尋、封存及恢復地圖，並依最近更新排序。
- D1 新增地圖擁有者、封存時間、建立時間與擁有者／更新時間索引。

### Changed

- 智慧整理會自動將結果調整至畫面中央，整次操作只建立一筆歷史，可直接復原與重做。
- 個人地圖沿用既有 `/m/:id` 編輯器與版本樂觀鎖；舊有無擁有者共享地圖維持相容。

### Security

- 個人地圖的列表、建立、更新、複製與編輯均在伺服器依 ChatGPT 使用者 email 驗證所有權；未授權查詢統一回傳找不到。

### Verified

- 正式建置與 lint 成功，33 項自動測試全數通過。
- 100 節點布局測試確認無節點重疊、父子方向清楚；分支布局確認根節點與未選取節點不移動。
- 本機 D1 migration 與 API 生命週期驗證通過：建立、搜尋、重新命名、複製、封存、分類與跨帳號隔離。
- 依要求未部署或發布正式網站。

## [0.10.1] - 2026-07-18

### Fixed

- 拖曳歷史精準化：單純點選節點不再產生無意義的復原紀錄；改為在放開節點且位置確實改變時才建立一筆 checkpoint。

### Verified

- 正式建置成功，25 項自動測試通過（含拖曳歷史精準化的原始碼護欄）。

## [0.10.0] - 2026-07-18

### Changed

- AI 面板改為「選節點、描述卡點、取得三個建議、點擊加入」的單一路徑。
- 建議卡片點擊後直接加入目前節點，不再需要多選與預覽確認。
- 網站由公開存取改為僅限擁有者的私人 Beta。

### Removed

- AI 模式切換、多節點上下文、節點對話歷史與批次建議流程。

### Verified

- 正式建置成功，25 項自動測試通過，並發布至 Sites 平台版本 17。

## [0.9.0] - 2026-07-16

### Added

- 伺服器端 OpenAI Responses API 與結構化建議輸出，API 金鑰僅存在伺服器環境。
- 發散、拆解、質疑三種 AI 思考模式。
- 最多 8 個節點的 AI 上下文選取，以及建議來源節點標示。
- 依地圖與節點保存在本機的 AI 討論紀錄、採用狀態與再次查看功能。
- AI 逾時、配額、金鑰、網路及輸出格式錯誤提示與重試操作。

### Changed

- 既有批次預覽流程改為同時支援真實 AI 建議與離線示範建議。
- 刪除節點或子樹時同步清除對應的本機 AI 討論紀錄。
- PRD 升級至 3.0，完成 M2 真 AI 基礎的 P1-01～P1-05 規格更新。

## [0.8.0] - 2026-07-16

### Changed

- 畫布縮放範圍由 70%–130% 擴大為 50%–200%。
- 左側工具列移除常駐中文小標，改為放大圖示並在滑鼠懸浮或鍵盤聚焦時顯示中文提示。
- 放大搜尋、節點操作、縮放、AI 建議、大綱排序及匯出選單圖示，提升辨識與觸控操作性。
- 手機版工具列寬度同步調整，避免放大後的圖示擁擠。
- PRD 升級至 2.3，記錄新版縮放範圍與圖示可用性規格。

## [0.7.0] - 2026-07-16

### Added

- 心智圖畫布支援滑鼠拖曳空白處平移，以及手機單指觸控平移。
- 大綱模式支援桌面拖放同層節點排序。
- 大綱模式加入手機友善的上移／下移按鈕，排序時保留完整子樹。

### Changed

- 「適合畫面」會同時清除手動平移量，快速找回中心主題。
- 節點拖曳改用位移計算，避免畫布平移後拖曳節點跳動。
- PRD 升級至 2.2，記錄跨裝置平移與結構化大綱排序的需求及驗收條件。

## [0.6.0] - 2026-07-15

### Added

- 畫布與大綱雙模式。
- 節點搜尋、分支收合與適合畫面。
- AI 建議多選、預覽與批次加入。
- 手機版可收合 AI bottom sheet。
- 可編輯文件標題；本機與共享地圖皆會保存。
- Enter、Tab、Delete/Backspace、Esc 等核心鍵盤操作。

### Changed

- 節點編輯由 `window.prompt` 改為內嵌表單。
- 強化選取狀態、觸控目標與手機版操作空間。

## [0.5.0] - 2026-07-15

### Added

- 無登入共享地圖與 `/m/[id]` 路由。
- Sites D1 儲存、版本樂觀鎖與衝突提示。
- 本機草稿自動保存、還原及重設。
- 心智圖純函式、儲存與共享資料測試。

## [0.4.0] - 2026-07-14

### Added

- PNG、PDF、Markdown 匯出。
- 漸細貝茲曲線連接線。
- 節點移除、復原與重做。

## [0.1.0] - 2026-07-13

### Added

- 靈感樹互動式心智圖初版。
- 節點新增、選取、拖曳、縮放與情境式 AI 建議原型。
- 溫暖紙張工作室視覺方向與 Sites 部署。
