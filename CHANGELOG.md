# 版本紀錄

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/) 的分類方式，產品版本採 [Semantic Versioning](https://semver.org/lang/zh-TW/)。Sites 平台版本與產品語意版本分開記錄。

## [Unreleased]

### Planned

- 大型心智圖持續效能改善、觸控與無障礙驗收。

## [0.17.0] - 2026-07-25

### Added

- 新增「靈感收件匣」抽屜，可將多行筆記、條列內容或快速輸入拆成尚未分類的種子。
- 種子支援標題／補充說明、來源標示、單筆移除、整批清空，以及選擇任一心智圖節點後「種到分支」。
- 新增收件匣 AI 腦力激盪；AI 依目前節點與使用者輸入方向產生 3–6 顆種子，不直接改動心智圖。
- 收件匣依本機地圖或雲端地圖 ID 使用版本化裝置儲存，重新整理後仍可繼續分類。

### Changed

- 多行輸入會清除常見項目符號、合併空白、辨識「標題｜補充」格式並過濾重複想法。
- 工具列新增收件匣入口與待分類數量；桌面採浮動抽屜，手機採保留 10px 邊界的可捲動面板。
- 將種子加入分支時沿用既有節點新增、復原、分支展開與自動保存流程。

### Verified

- `npm run lint`、正式建置、`git diff --check` 與 49 項自動測試全數通過。
- 實際完成「貼上三行（含一筆重複）→ 產生兩顆種子 → 種到身心健康分支 → 可復原」，並確認重新整理後種子仍存在。
- 真實 AI 成功產生六顆不重複種子；手機 390×844 面板完整位於視窗內且無水平溢位。

## [0.16.0] - 2026-07-25

### Added

- 樹狀檢視支援拖曳節點微調樹冠；每個節點使用獨立的檢視偏移量，切回心智圖時不改變原本自由畫布座標。
- 樹狀節點移動時，主幹與分枝會依新的顯示座標同步重繪；「智慧整理」可清除手動偏移並恢復自動樹冠。

### Changed

- 心智圖與樹狀拖曳改為以 `requestAnimationFrame` 合併高頻指標更新，每個畫面更新週期最多寫入一次 React 狀態。
- 拖曳期間停用節點 `left/top` 座標動畫，放開滑鼠後不再出現卡片追趕游標的延遲。
- 加入 3px 移動門檻、`pointerdown` 預設行為阻擋與拖曳中全畫布文字選取防護；雙擊編輯與單一節點選取仍維持正常。
- 樹狀節點游標與操作提示改為可拖曳狀態，明確說明可微調樹冠。

### Verified

- `npm run lint`、正式建置、`git diff --check` 與 46 項自動測試全數通過。
- 桌面 1280×720 實際驗證心智圖節點立即移動、單步復原、樹狀節點與枝條同步、模式座標隔離、偏移保留及智慧整理重設。
- 拖曳後只保留一個選取節點，瀏覽器文字選取為空；雙擊編輯正常，主控台無警告或錯誤。

## [0.15.0] - 2026-07-25

### Added

- AI 思考助手新增「自動擴寫」，依目前節點、上層脈絡與既有子節點生成 3–6 個互補草稿。
- 擴寫草稿可逐一加入或一次全部加入；整組加入會自動整理畫布，且只建立一筆可復原歷史。
- 新增「概念解讀」，產生白話定義、核心重點、圖中節點關聯與延伸問題。
- 概念解讀的精簡摘要可直接設為節點說明，並支援復原。

### Changed

- AI 端點依擴寫與解讀任務使用不同嚴格 JSON Schema，伺服器會驗證輸出長度、節點 ID 與必要欄位。
- AI 提示納入祖先節點及既有子節點，擴寫會明確避免同義重複；解讀資訊不足時要求以保留語氣表達。
- AI 面板維持原有視覺系統，桌面使用雙模式控制，手機沿用可收合 bottom sheet。

### Verified

- `npm run lint`、正式建置與 45 項自動測試全數通過。
- 本機以真實 Responses API 完成擴寫與解讀請求；實際驗證整組加入 5 個節點、單步復原、摘要寫入與復原。
- 桌面與手機 390×844 介面無水平溢位，瀏覽器主控台無相關警告或錯誤。
- 本版尚未發布到正式站，待確認後再同步 GitHub 主分支與 Sites 正式版本。

## [0.14.0] - 2026-07-25

### Added

- 新增「移植分支」操作，可將整個分支改掛到另一個父節點；選擇面板會排除自己與所有後代，避免形成循環。
- 大綱模式新增縮排與凸排，可直接調整分支層級，並保留同層順序與完整後代。

### Changed

- 移植、縮排與凸排完成後會自動整理節點位置、展開新的父節點並保持移動節點為目前選取項目。
- 每次結構變更只建立一筆歷史紀錄，可完整復原與重做。
- 手機版大綱操作改為固定寬度的 2×2 結構按鈕，避免窄螢幕水平溢位。

### Verified

- `npm run lint`、正式建置與 42 項自動測試全數通過。
- 桌面 1280×720 與手機 390×844 實際完成跨分支移植、循環目標排除、復原／重做、縮排與凸排；手機版無水平溢位，瀏覽器主控台無警告或錯誤。
- 本版尚未發布到正式站，待確認後再同步 GitHub 主分支與 Sites 正式版本。

## [0.13.0] - 2026-07-24

### Added

- 新增「樹狀」檢視，將中心主題置於底部，依階層向上生成主幹、分枝與葉冠。
- 三模式切換列提供心智圖、樹狀與大綱；手機版同樣保留完整切換、搜尋與整理入口。
- 樹狀模式的 PNG／PDF 匯出會使用目前樹形布局與由粗至細的分枝。

### Changed

- 樹狀布局由原始節點資料即時計算，不覆寫自由畫布座標，也不額外建立復原紀錄。
- 樹狀節點不可任意拖離枝條，但保留選取、編輯、新增、收合、搜尋、縮放、平移與智慧整理。
- 樹狀連接線改為共用主幹與分段剝離的彩色枝條；外側粗枝較早分出、內側枝條沿主幹向上延伸，第一層節點同步高低錯落，避免線條穿越相鄰卡片。

### Verified

- 正式建置成功，40 項自動測試全數通過；涵蓋樹狀布局向上生長、無重疊、資料不變、100 節點決定性、共用主幹與分段分枝。
- 桌面 1536×1024 與手機 390×844 實際驗證完成；搜尋、分枝收合／展開與就地編輯正常，瀏覽器主控台無錯誤。
- 與使用者提供的樹形連接線附件並排比較後，完成主幹粗細、分枝高度、垂直節奏與畫面留白修正。

## [0.12.0] - 2026-07-24

### Added

- `Cmd/Ctrl+Z` 鍵盤復原，以及 `Cmd/Ctrl+Shift+Z`、`Ctrl+Y` 鍵盤重做；文字輸入框保留原生文字歷史。
- 雲端地圖的裝置端離線草稿保護，以地圖 ID 隔離未同步內容，重開頁面可自動復原。
- 同步失敗與離線狀態提示，提供手動重新同步，恢復連線後也會自動重試。

### Changed

- 雲端保存改為先寫入安全草稿再送出；同步期間若持續編輯，會在目前請求完成後接續保存最新版本。
- 成功同步或選擇載入衝突最新版後會清除安全草稿，避免舊內容重複覆蓋。

### Verified

- 正式建置成功，36 項自動測試全數通過。
- 實際操作新增節點、鍵盤復原與鍵盤重做均成功；桌面與 390×844 手機版無水平溢位，瀏覽器主控台無錯誤。
- 發布至 Sites 平台版本 21，並以 `v0.12.0` 標籤同步 GitHub 主分支。

## [0.11.1] - 2026-07-24

### Changed

- 節點的編輯、收合、智慧整理與新增操作改為卡片內的緊湊操作列，不再突出節點邊界或擠壓標題。
- 桌面圓形按鈕縮為 24px；手機維持 28px 視覺尺寸，並在滑入、選取或鍵盤聚焦時提高對比。

### Verified

- 正式建置、lint 與 33 項自動測試全數通過。
- 桌面 1280×720 與手機 390×844 視覺驗證通過；取樣節點的操作列皆完整位於節點方塊內。
- 編輯與新增操作已在本機預覽實際驗證，瀏覽器主控台無錯誤。

## [0.11.0] - 2026-07-24

### Added

- 「智慧整理」可一鍵重排整張心智圖；節點快捷操作可只整理指定分支。
- 雙向整潔樹布局會固定中心或分支根節點、延續既有左右方向，並避開未整理的節點。
- `/maps` 個人地圖工作區，可建立、重新命名、複製、搜尋、封存及恢復地圖，並依最近更新排序。
- D1 新增地圖擁有者、封存時間、建立時間與擁有者／更新時間索引。

### Changed

- 智慧整理會自動將結果調整至畫面中央，整次操作只建立一筆歷史，可直接復原與重做。
- 個人地圖沿用既有 `/m/:id` 編輯器與版本樂觀鎖；舊有無擁有者共享地圖維持相容。

### Security

- 個人地圖的列表、建立、更新、複製與編輯均在伺服器依 ChatGPT 使用者 email 驗證所有權；未授權查詢統一回傳找不到。

### Verified

- 正式建置與 lint 成功，33 項自動測試全數通過。
- 100 節點布局測試確認無節點重疊、父子方向清楚；分支布局確認根節點與未選取節點不移動。
- 本機 D1 migration 與 API 生命週期驗證通過：建立、搜尋、重新命名、複製、封存、分類與跨帳號隔離。
- 依要求未部署或發布正式網站。

## [0.10.1] - 2026-07-18

### Fixed

- 拖曳歷史精準化：單純點選節點不再產生無意義的復原紀錄；改為在放開節點且位置確實改變時才建立一筆 checkpoint。

### Verified

- 正式建置成功，25 項自動測試通過（含拖曳歷史精準化的原始碼護欄）。

## [0.10.0] - 2026-07-18

### Changed

- AI 面板改為「選節點、描述卡點、取得三個建議、點擊加入」的單一路徑。
- 建議卡片點擊後直接加入目前節點，不再需要多選與預覽確認。
- 網站由公開存取改為僅限擁有者的私人 Beta。

### Removed

- AI 模式切換、多節點上下文、節點對話歷史與批次建議流程。

### Verified

- 正式建置成功，25 項自動測試通過，並發布至 Sites 平台版本 17。

## [0.9.0] - 2026-07-16

### Added

- 伺服器端 OpenAI Responses API 與結構化建議輸出，API 金鑰僅存在伺服器環境。
- 發散、拆解、質疑三種 AI 思考模式。
- 最多 8 個節點的 AI 上下文選取，以及建議來源節點標示。
- 依地圖與節點保存在本機的 AI 討論紀錄、採用狀態與再次查看功能。
- AI 逾時、配額、金鑰、網路及輸出格式錯誤提示與重試操作。

### Changed

- 既有批次預覽流程改為同時支援真實 AI 建議與離線示範建議。
- 刪除節點或子樹時同步清除對應的本機 AI 討論紀錄。
- PRD 升級至 3.0，完成 M2 真 AI 基礎的 P1-01～P1-05 規格更新。

## [0.8.0] - 2026-07-16

### Changed

- 畫布縮放範圍由 70%–130% 擴大為 50%–200%。
- 左側工具列移除常駐中文小標，改為放大圖示並在滑鼠懸浮或鍵盤聚焦時顯示中文提示。
- 放大搜尋、節點操作、縮放、AI 建議、大綱排序及匯出選單圖示，提升辨識與觸控操作性。
- 手機版工具列寬度同步調整，避免放大後的圖示擁擠。
- PRD 升級至 2.3，記錄新版縮放範圍與圖示可用性規格。

## [0.7.0] - 2026-07-16

### Added

- 心智圖畫布支援滑鼠拖曳空白處平移，以及手機單指觸控平移。
- 大綱模式支援桌面拖放同層節點排序。
- 大綱模式加入手機友善的上移／下移按鈕，排序時保留完整子樹。

### Changed

- 「適合畫面」會同時清除手動平移量，快速找回中心主題。
- 節點拖曳改用位移計算，避免畫布平移後拖曳節點跳動。
- PRD 升級至 2.2，記錄跨裝置平移與結構化大綱排序的需求及驗收條件。

## [0.6.0] - 2026-07-15

### Added

- 畫布與大綱雙模式。
- 節點搜尋、分支收合與適合畫面。
- AI 建議多選、預覽與批次加入。
- 手機版可收合 AI bottom sheet。
- 可編輯文件標題；本機與共享地圖皆會保存。
- Enter、Tab、Delete/Backspace、Esc 等核心鍵盤操作。

### Changed

- 節點編輯由 `window.prompt` 改為內嵌表單。
- 強化選取狀態、觸控目標與手機版操作空間。

## [0.5.0] - 2026-07-15

### Added

- 無登入共享地圖與 `/m/[id]` 路由。
- Sites D1 儲存、版本樂觀鎖與衝突提示。
- 本機草稿自動保存、還原及重設。
- 心智圖純函式、儲存與共享資料測試。

## [0.4.0] - 2026-07-14

### Added

- PNG、PDF、Markdown 匯出。
- 漸細貝茲曲線連接線。
- 節點移除、復原與重做。

## [0.1.0] - 2026-07-13

### Added

- 靈感樹互動式心智圖初版。
- 節點新增、選取、拖曳、縮放與情境式 AI 建議原型。
- 溫暖紙張工作室視覺方向與 Sites 部署。
