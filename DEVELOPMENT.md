# 靈感樹開發紀錄

## 專案摘要

- 專案名稱：靈感樹｜AI 心智圖工作室
- 初版完成日期：2026-07-13
- 開發紀錄建立日期：2026-07-14
- 產品目標：讓使用者自由整理想法，並透過 AI 自動擴寫與概念解讀持續推進、理解思路。
- 線上版本：https://inspiration-tree-ai-mindmap.go38.chatgpt.site
- 目前產品版本：v0.29.0
- 存取狀態：公開 Beta

## 文件分工

- [PRD.md](./PRD.md)：產品需求、驗收條件與非功能需求。
- [ROADMAP.md](./ROADMAP.md)：下一步順序與里程碑。
- 本文件：按日期記錄決策、實作、驗證與部署證據。
- [CHANGELOG.md](./CHANGELOG.md)：使用者可感知的版本變更。

## 設計決策

初期比較了三個視覺方向：

1. 溫暖紙張編輯工作室
2. 深靛 AI 聚焦畫布
3. 瑞士知識工作台

最終採用「溫暖紙張編輯工作室」。主色為奶油白、墨黑、珊瑚橘、鼠尾草綠與淡黃色；畫布使用細緻點狀網格，節點則以柔和紙張卡片呈現。整體希望讓長時間思考感覺安定、親切，同時保留清楚的資訊層級。

## 第一版功能

- 顯示具有父子關係與連線的心智圖。
- 點選節點以切換目前聚焦內容。
- 拖曳節點調整位置。
- 雙擊節點編輯標題與補充說明。
- 從工具列、節點快捷按鈕或 AI 建議新增子節點。
- 提供 70% 至 130% 畫布縮放及快速回到 100%。
- 支援節點新增、編輯與拖曳操作的復原。
- AI 協作區依目前節點顯示延伸建議。
- 「換一組靈感」可輪替不同建議組合，並保留目前聚焦節點的情境。
- 提供「拆解步驟」、「找出盲點」及「換個角度」提示捷徑。
- 可輸入自然語言並將內容轉為新的延伸節點。
- 針對桌面、平板與窄螢幕提供響應式版面。
- 支援降低動態效果的系統偏好。
- 可將目前心智圖匯出為保留階層與說明的 Markdown 檔案。
- 可將目前完整畫布、節點及連線匯出為橫式 PDF 檔案。

## 初版 AI 功能範圍

初版 AI 體驗屬互動式原型。AI 建議來自前端內建的情境範例，使用者輸入會依目前聚焦節點整理成新節點。此限制已於 v0.9.0 解決；目前保留內建建議作為未設定金鑰時的離線備援。

未來若要提供真正的生成式 AI，可新增伺服器端 API，將目前節點、祖先路徑與使用者提示作為上下文，要求模型回傳結構化的節點建議。API 金鑰必須保留在伺服器端，不可放入瀏覽器程式碼。

## 技術架構

- React 19 與 TypeScript
- Next.js 相容的 vinext 執行環境
- Vite 建置流程
- CSS 自製畫布、節點、連線及響應式介面
- Sites 私人託管

主要產品介面位於 `app/page.tsx`，視覺樣式位於 `app/globals.css`，網站中繼資料與語言設定位於 `app/layout.tsx`。

## 驗證與發布

- 已執行正式建置，結果成功。
- 2026-07-13 建立並發布 Sites 私人版本 1。
- 發布網址：https://inspiration-tree-ai-mindmap.go38.chatgpt.site
- 初版提交：`831842a`（Build AI mind map studio）

## 2026-07-15｜公開 Beta 開發紀錄

### 需求治理

- PRD 2.1 將核心工作流整理為 P0／P1／P2 backlog，為每項功能建立需求 ID、狀態、驗收條件、依賴與建議里程碑。
- P0 聚焦可靠性與大型心智圖可用性，P1 聚焦真 AI 與個人生產力，P2 聚焦分享權限、版本與多人協作。

### 資料與分享

- 完成本機草稿自動保存與還原，並以獨立鍵值保存自訂文件標題。
- 完成 Sites D1 無登入共享地圖、`/m/[id]` 共享頁、樂觀鎖與衝突處理。
- 正上方文件標題可直接修改；共享模式會同步保存新標題。

### 核心工作流

- 節點改為內嵌編輯，支援 Enter 儲存與 Esc 取消。
- 新增 Enter 同層、Tab 子節點及 Delete/Backspace 移除快捷鍵。
- 新增畫布／大綱雙模式、節點搜尋、分支收合與適合畫面。
- AI 建議改為可多選、預覽並批次加入。
- 手機 AI 助手改為可收合 bottom sheet，降低畫布遮擋。

### 驗證與發佈

- 重寫 starter 殘留測試，建立心智圖、儲存、共享資料與 SSR 測試護欄。
- `npm run build` 成功，19 項自動測試通過。
- Sites 已公開發佈至第 11 版；產品語意版本定為 `v0.6.0`。

## 2026-07-16｜v0.7.0 畫布導覽與大綱排序

### 核心工作流

- 畫布空白區可使用滑鼠拖曳平移，手機可用單指觸控平移，改善窄螢幕找不到中心主題的問題。
- 「適合畫面」會重設使用者平移量並重新計算縮放與置中。
- 節點拖曳改為以游標位移量換算座標，避免畫布平移後節點跳位。
- 大綱模式支援桌面原生拖放同層排序；手機提供明確的上移／下移按鈕。
- 排序只調整同一父節點下的順序，完整保留子樹與既有資料格式，並納入復原歷史。

### 驗證

- `npm run build` 成功。
- 20 項自動測試全數通過，新增同層排序、邊界與跨父節點保護測試。
- 本機視覺驗證受執行環境連接埠權限限制（`EPERM 0.0.0.0:9229`），待 Sites 預覽版本補做實機操作驗證。

## 2026-07-16｜v0.8.0 縮放與圖示可用性

### 介面調整

- 畫布縮放上下限調整為 50% 與 200%，維持每次 10% 的可預期級距。
- 左側工具列改為純大圖示，桌面以懸浮提示、鍵盤以聚焦提示提供中文功能名稱，無障礙名稱維持完整。
- 同步放大標題編輯、搜尋、節點快捷操作、縮放、AI 建議、大綱排序與匯出類型圖示。
- 手機工具列加寬至 60px，維持放大圖示的觸控空間，並避免顯示不適合觸控情境的懸浮提示。

### 驗證

- 正式建置成功，20 項自動測試全數通過。
- 新增 50%／200% 縮放界線與工具列提示的原始碼測試護欄。
- lint 顯示 3 項既有規則問題，位於自動保存 effect、草稿還原 effect 與共享頁連結，與本次介面調整無關。

## 2026-07-16｜v0.9.0 真 AI 思考基礎

### AI 核心

- 新增伺服器端 `/api/suggest`，透過 OpenAI Responses API 取得結構化建議；金鑰只讀取伺服器環境變數。
- 預設模型採成本敏感型 `gpt-5.6-luna`，並可用 `OPENAI_MODEL` 調整。
- 完成發散、拆解、質疑三種思考模式；建議仍沿用現有多選、預覽與批次加入流程。
- 可選擇最多 8 個上下文節點，AI 建議會帶回來源節點 ID，介面顯示來源名稱。
- AI 討論依地圖與節點保存在本機，切換後可回看摘要、建議與採用數量；刪除節點會同步清除相關紀錄。
- 加入 25 秒逾時、配額、金鑰、網路、欄位長度、來源 ID 與結構化輸出驗證。

### 驗證與啟用條件

- 正式建置成功，24 項自動測試全數通過。
- 線上真實 AI 需由管理者在 Sites 設定 `OPENAI_API_KEY`；未設定時會顯示明確提示並保留離線示範建議。
- 節點對話目前為裝置本機保存；跨裝置雲端同步列為後續增強。

## 2026-07-18｜v0.10.0 AI 體驗簡化與私人發布

### 產品決策

- 原 AI 面板同時呈現三種模式、多節點上下文、對話歷史、多選與批次預覽，增加認知負擔並干擾思考。
- 改採單一路徑：「選擇節點 → 描述卡點或留白 → 取得三個建議 → 點一下加入」。
- 進階上下文與歷史不再常駐；只有使用者研究證明必要時才會以漸進揭露方式重新評估。

### 實作

- 移除 AI 模式切換、多節點上下文、節點對話歷史、多選、批次預覽與快捷提示列。
- 將提示輸入移到焦點節點下方，使用更直接的「你卡在哪裡？」文案。
- 建議卡片改為單擊立即加入目前節點，並沿用既有復原機制。
- 更新 SSR 與原始碼測試，防止已移除的複雜控制重新出現。

### 驗證與部署

- 因專案目錄逐檔讀取相依套件異常緩慢，使用乾淨的本機暫存副本與 Node 22 完成可重現建置。
- 正式建置成功，25 項自動測試全數通過。
- 提交 `009b14e`，Sites 平台版本 17 發布成功。
- 網站存取由公開改為自訂名單，目前僅允許擁有者 `eric.hsin@gmail.com`。

## 2026-07-18｜v0.10.1 拖曳歷史精準化

### 問題

- 節點 `onPointerDown` 會無條件呼叫 `checkpoint()`，導致單純點選節點（未拖曳）也產生一筆復原紀錄，累積後淹沒真正的編輯步驟。

### 實作

- 移除按下即建立 checkpoint 的行為；改為在 `onPointerDown` 只記錄拖曳起點與「拖曳前快照」（`before: { nodes, selectedId }`）。
- `onPointerMove` 只有在換算後座標與起點不同時才更新節點並標記 `moved`；純點選（座標未變）完全不動用狀態。
- `endPointerInteraction`（放開）時，僅在 `moved` 為真時把拖曳前快照推入歷史，確保單次拖曳只新增一筆 checkpoint。

### 驗證

- 新增原始碼護欄：確認按下不再 checkpoint、拖曳前快照與 `moved` 判斷存在。
- 正式建置成功，25 項自動測試全數通過。
- 對應需求 HIS-05、P0-11；ROADMAP P0 第 1 項完成。

## 2026-07-24｜v0.11.0 智慧布局與個人地圖工作區

### 智慧自動布局（P0-10）

- 在 `app/lib/mindmap.ts` 實作不依賴 React／DOM 的雙向整潔樹布局，中心節點固定不動，第一層分支保留明確的既有左右方向；位置含糊時平均分配至兩側。
- 指定分支布局固定分支根節點，只重排後代，並計算與未選取節點的碰撞區間，以最小垂直位移避讓外部節點。
- 畫布加入「智慧整理」全圖入口；有子節點的節點快捷操作可只整理該分支。
- 每次布局先建立單一歷史 checkpoint，布局結果可使用既有復原／重做機制還原；完成後同步適合畫面。
- 節點位置轉場沿用既有低干擾互動，系統偏好降低動態效果時由現有 CSS 護欄停用動畫。

### 個人地圖工作區（P1-06）

- 新增 `/maps` 身分保護頁，延續 dispatch-owned ChatGPT 身分與 `oai-authenticated-user-email`，未登入時走既有 SIWC 流程。
- D1 `mind_maps` 新增 `owner_email`、`archived_at`、`created_at`，以及 `owner_email + archived_at + updated_at` 索引；migration 為 `0001_calm_tempest.sql`。
- 新增 `/api/workspace/maps` 與 `/api/workspace/maps/:id`：列出、搜尋、建立、複製、重新命名、封存與恢復都以伺服器端 owner 條件查詢。
- 個人地圖沿用既有 `/m/:id` 編輯器、JSON 節點資料與版本樂觀鎖；`/api/maps/:id` 同步加入所有權條件，跨帳號存取不洩漏地圖是否存在。
- `owner_email IS NULL` 的既有共享地圖維持原本的不可猜測連結模式，避免 migration 破壞既有分享流程。
- 個人工作區介面依最近更新排序，提供使用中／已封存分頁、即時名稱搜尋、內嵌重新命名與手機響應式操作。

### 驗證

- `npm run build` 成功，新增 `/maps`、`/api/workspace/maps`、`/api/workspace/maps/:id` 路由。
- `npm run lint` 成功。
- 33 項自動測試全數通過；新增 100 節點無重疊、階層方向、分支錨定／避讓、所有權、payload 驗證、建立節點與版本長度測試。
- 本機 D1 依序套用 `0000`、`0001` migration 成功。
- 本機 API 冒煙測試完成建立 → 搜尋 → 重新命名 → 複製 → 封存 → 使用中／封存分類；不同帳號讀取個人地圖回傳 404。
- 這是協調任務的本地實作，依要求未建立 Sites 版本、未部署或發布正式網站。

## 2026-07-24｜v0.11.1 節點內嵌緊湊操作列

### 設計與實作

- 將節點的編輯、收合、智慧整理與新增按鈕從文字旁或卡片外側移到節點內部右下方。
- 桌面按鈕縮為 24px，手機維持 28px；按鈕列使用既有紙張、墨黑與珊瑚色 token，不引入新的視覺語言。
- 節點預留操作列空間，避免按鈕遮住標題與說明；滑入、選取或鍵盤聚焦時提高按鈕對比。
- 新增 `design-qa.md`，保留桌面、手機、前後全畫面與節點細節比較證據。

### 驗證與發布

- `npm test` 成功，正式建置與 33 項自動測試全數通過。
- `npm run lint` 與 `git diff --check` 成功。
- 桌面 1280×720 與手機 390×844 畫面驗證通過；DOM 幾何檢查確認取樣操作列均位於節點邊界內。
- 編輯與新增按鈕已實際操作，瀏覽器主控台無錯誤。
- 發布至 Sites 平台版本 20，來源與 GitHub `main` 使用同一個 commit。

## 2026-07-24｜v0.12.0 編輯可靠性

### 設計與實作

- 加入 macOS 與 Windows 慣用的鍵盤歷史操作：`Cmd/Ctrl+Z` 復原、`Cmd/Ctrl+Shift+Z` 與 `Ctrl+Y` 重做；游標位於文字輸入框時保留原生文字復原。
- 雲端地圖每次修改會立即寫入以地圖 ID 隔離的裝置草稿，再以 800ms debounce 同步 D1；草稿保存標題、節點、選取節點、基準版本與更新時間。
- 同步失敗或離線時保留草稿並顯示影響與重試入口；恢復連線後自動重試，重開頁面可復原未同步內容。
- 同步請求期間若又有新修改，以 revision 與單一 in-flight request 排隊接續同步，避免並行版本競爭。
- 成功同步或選擇載入衝突最新版後清除裝置草稿；裝置儲存不可用時仍允許線上同步。

### 驗證與發布

- 正式建置成功，36 項自動測試全數通過；新增跨平台快捷鍵與雲端草稿格式、驗證及地圖隔離測試。
- 本機預覽實際完成新增、鍵盤復原與鍵盤重做；結果由 7 → 8 → 7 → 8 個節點正確切換，最後還原測試資料。
- 桌面與 390×844 手機版驗證無水平溢位；主控台無錯誤，既有版面、節點操作列與行動版 AI 面板維持原設計。
- 發布至 Sites 平台版本 21，來源與 GitHub `main` 使用同一個 commit。

## 2026-07-24｜v0.13.0 自然樹冠檢視

### 設計與實作

- 依選定的方案 2「自然葉冠」加入第三種檢視：中心主題位於底部，主幹向上分岔，節點沿枝條錯落生長。
- 新增純函式樹狀布局：以葉節點槽位決定水平位置、父節點置於後代範圍中央、階層向上排列，並以輕微錯落維持有機節奏。
- 多子節點使用一段共用中性色主幹；彩色枝條依水平距離在不同高度從主幹剝離，外枝較低、內枝較高，並以由粗至細的封閉 SVG ribbon 呈現。
- 第一層節點依樹冠位置高低錯落，增加根部到枝端的垂直生長距離，也避免跨越相鄰節點卡片。
- 樹狀位置只在顯示時衍生，不寫入節點資料、不覆寫自由畫布座標，也不增加復原紀錄。
- 樹狀檢視保留搜尋、選取、就地編輯、新增、分支收合、智慧整理、縮放、平移與 AI 聚焦；節點固定在枝條上，避免拖曳破壞樹形。
- PNG／PDF 會依目前檢視匯出樹形布局，並使用由主幹到末梢逐層變細的曲線。
- 手機版命令列改為兩列配置，完整保留三模式切換、搜尋、智慧整理與適合畫面。

### 驗證

- `npm test` 成功，正式建置與 40 項自動測試全數通過。
- 新增小型樹與 100 節點樹的向上生長、無重疊、決定性及不改寫來源資料測試。
- 桌面 1536×1024 與手機 390×844 樹狀畫面驗證通過；搜尋、收合／展開與節點就地編輯均已實際操作。
- 與方案 2 視覺基準並排比較後，加強主幹粗細、拉開垂直層級並限制自動放大，保留自然樹冠留白；主控台無錯誤。
- 本版尚未發布到正式站，待使用者確認後再同步 GitHub 主分支與 Sites 正式版本。

## 2026-07-25｜v0.14.0 跨分支移植與層級調整

### 設計與實作

- 節點與大綱加入「移植分支」入口，以明確面板選擇新的父節點；根節點不可移植，來源節點與所有後代不列入候選，從介面與資料層雙重阻止循環。
- `app/lib/mindmap.ts` 新增純函式 `reparentSubtree`、`indentOutlineNode` 與 `outdentOutlineNode`，改掛時保留完整後代、同層順序與樹狀先序資料排列。
- 大綱模式提供縮排與凸排；縮排改掛到前一個同層節點下方，凸排則移到目前父節點之後，結果符合常見大綱編輯器的層級直覺。
- 結構變更整合既有智慧布局、分支展開、目前選取與 toast 回饋；每次操作只建立一筆 checkpoint，能以既有復原／重做完整往返。
- 手機版隱藏較次要的文字操作，將凸排、縮排、上移、下移整理為 2×2 控制，維持 390px 窄螢幕可用性。

### 驗證

- `npm run lint` 與 `npm test` 成功，正式建置及 42 項自動測試全數通過。
- 純函式測試涵蓋完整子樹移植、根／自己／後代／遺失目標拒絕、縮排／凸排順序與不可操作情境。
- 桌面 1280×720 實際將「身心健康」及其子節點「晨間散步」移植到「創意工作」，確認循環候選已排除，復原與重做均能回到正確階層。
- 大綱模式實際完成「關係與連結」縮排與凸排；手機 390×844 的移植面板完整位於視窗內，結構控制無水平溢位。
- 驗證後已清除測試草稿並還原預設範例；瀏覽器主控台無警告或錯誤。
- 本版尚未發布到正式站，待使用者確認後再同步 GitHub 主分支與 Sites 正式版本。

## 2026-07-25｜v0.15.0 AI 自動擴寫與概念解讀

### 設計與實作

- AI 面板改為兩個清楚任務：「自動擴寫」負責延伸思路，「概念解讀」負責快速理解目前節點；保留單一輸入框與目前節點焦點，不加入複雜上下文設定。
- 擴寫提示會自動帶入祖先路徑、目前節點與既有子節點，要求 3–6 個互補且不重複的子概念；結果可逐一加入，或以一次 checkpoint 整組加入並自動布局。
- 概念解讀以獨立結構化輸出回傳精簡摘要、白話定義、2–4 個核心重點、既有節點關聯與一個延伸問題；資訊不足時要求明確保留推測語氣。
- 使用者可將精簡摘要設為節點說明；寫入、整組擴寫與後續布局均沿用既有草稿保存、雲端同步及復原／重做機制。
- `/api/suggest` 依任務選用不同嚴格 JSON Schema，伺服器驗證必要欄位、內容長度及關聯節點 ID；金鑰只存在伺服器環境。
- 保留 `gpt-5.6-luna` 作為大量、低延遲心智圖輔助的預設模型，並使用低 reasoning effort 與低 verbosity；未任意升級至高成本旗艦模型。
- 修正連續操作 toast 的計時器競爭，新的操作提示會先取消上一個清除計時器。

### 驗證

- `npm run lint`、正式建置與 45 項自動測試全數通過；新增擴寫脈絡、避免重複、概念解讀提示與解讀輸出驗證測試。
- 本機以真實 OpenAI Responses API 驗證：擴寫成功產生 5 個互補節點；解讀成功回傳定義、4 個重點、兩個既有節點關聯及延伸問題。
- 桌面實際完成「生成擴寫 → 全部加入 5 個節點 → 復原」，節點數由 7 → 12 → 7；「生成解讀 → 設為節點說明 → 復原」亦正確回到原說明。
- 手機 390×844 bottom sheet 完整顯示雙模式、輸入與結果入口，面板可捲動且文件寬度維持 390px；主控台無相關警告或錯誤。
- 驗證後測試節點已復原；本版尚未發布到正式站，待使用者確認後再同步 GitHub 主分支與 Sites 正式版本。

## 2026-07-25｜v0.16.0 拖曳效能、選取防護與樹狀微調

### 問題與決策

- 心智圖節點在拖曳時雖然資料座標已更新，CSS 的 `left/top 0.24s` 動畫仍讓卡片追趕游標，形成明顯延遲；每個原始 `pointermove` 直接更新 React 狀態也會放大大型地圖的重繪成本。
- 樹狀檢視原本在節點 `pointerdown` 直接停止拖曳，且顯示座標由階層布局衍生；直接修改節點 `x/y` 不但看不到結果，也會污染自由畫布位置。
- 採用「高頻指標放入 ref、每個動畫幀最多提交一次」的拖曳策略；樹狀微調採每節點獨立偏移量，套用在自動布局之後，避免改變既有節點資料與樹形拓撲。

### 實作

- 心智圖與畫布平移共用 `requestAnimationFrame` 排程，最新指標位置保存在暫時 ref；放開時同步 flush 最後座標，避免最後一幀遺失。
- 拖曳節點加入 `.dragging` 狀態，移除 `left/top` 座標轉場，只保留陰影與 transform 回饋；加入 3px 移動門檻、`pointerdown.preventDefault()` 與拖曳中 `user-select: none`。
- `TreeNodeOffsets` 與 `applyTreeNodeOffsets` 將樹狀偏移套用在自動布局複本；心智圖仍讀取原始 `x/y`。樹狀節點移動會使 `createTreeBranchRibbons` 依顯示座標同步重算枝條。
- 樹狀模式改用 grab／grabbing 游標並顯示「拖曳節點微調樹冠」提示；切換檢視會保留本次工作階段的偏移，「智慧整理」可重設整棵樹或指定分支的手動偏移。
- 刪除節點時同步清除無效偏移；載入衝突最新版或重設範例時清空樹狀偏移，避免套用到不同資料。

### 驗證與發布準備

- `npm run lint`、正式建置、`git diff --check` 與 46 項自動測試全數通過；新增樹狀偏移不修改來源座標的純函式測試與原始碼護欄。
- 桌面 1280×720 實際拖曳「身心健康」：心智圖畫面座標立即由 `(96, 248.125)` 移至 `(216, 328.125)`，與 120×80 指標位移一致，不再等待 0.24 秒追趕。
- 樹狀節點由 `(132, 410)` 拖至 `(252, 330)`，枝條 SVG 路徑同步改變；切回心智圖仍維持原始 `(130, 125)`，再切回樹狀則保留偏移。
- 「智慧整理」可將樹狀節點恢復到自動布局 `(132, 410)`；拖曳後只有一個節點選取、文字選取為空，雙擊編輯正常，主控台無警告或錯誤。
- 本次正式發布將累積納入 v0.14.0 跨分支移植、v0.15.0 AI 擴寫／概念解讀與 v0.16.0 拖曳改善。

## 2026-07-25｜v0.17.0 靈感收件匣與 AI 腦力激盪

### 問題與決策

- 零碎想法常在尚未知道歸屬時出現；若要求使用者先選對分支才能記錄，容易中斷思考，直接建立節點也會讓畫布快速變亂。
- 收件匣定位為「正式節點之前的待整理層」：種子與心智圖座標、階層及雲端版本分離，只有使用者明確選擇分支並種下時才建立節點。
- 收件匣依地圖範圍保存在裝置端；不把私人草稿自動送上雲端。AI 僅接收目前節點脈絡與使用者主動輸入的方向。

### 實作

- 左側工具列新增收件匣入口與數量徽章；桌面使用浮動抽屜，手機改為視窗內可捲動面板。
- 多行解析會清除項目符號與編號、辨識「標題｜補充」格式、限制標題／說明長度並依標題去重；每張地圖最多保留 80 顆種子。
- `app/lib/inbox.ts` 封裝純函式與版本化 `localStorage` 契約，能安全忽略損壞、重複或過期格式的資料。
- 種子可選擇任一節點作為目的分支；「種到分支」沿用既有 `addNode`、checkpoint、展開與自動保存流程。
- 「AI 幫我想」重用既有 `/api/suggest` 的發散模式與嚴格結構化輸出，將 3–6 個建議轉為 AI 種子，不直接更動心智圖。

### 驗證與發布準備

- `npm run lint`、正式建置、`git diff --check` 與 49 項自動測試全數通過；新增多行解析、去重、AI 種子與版本化保存測試。
- 實際貼上三行內容（含一筆重複），正確形成兩顆種子；其中一顆種到「身心健康」後建立正式節點，且復原入口立即可用。
- 真實 AI 依目前脈絡產生六顆種子，全部保留在收件匣等待分類；重新整理後數量恢復。
- 手機 390×844 檢查面板邊界為 left 70、right 380、top 86、bottom 834，文件沒有水平溢位。

## 2026-07-25｜v0.18.0 連線可讀性與環狀智慧布局

### 問題與決策

- 原全圖智慧整理只把分支放在中心左右兩側，大型或多分支地圖容易形成兩條密集直欄；連線又使用依 ID 交替彎折的曲線，可能在節點移動後產生不必要的交錯。
- 全圖整理改採巢狀環狀扇區：葉節點平均分配角度，父節點位於後代角度區間中央，各層半徑依最小角距自動擴張。不同子樹因此保有獨立扇區，不需增加新的資料欄位。
- 指定分支整理仍保留既有局部欄式算法，避免移動未選取節點；只有全圖「智慧整理」採環狀分布。

### 實作

- 心智圖連線改從父子卡片邊界的射線交點出入，移除交替左右彎折；同一節點的不同分支不再全部堆疊於卡片中心。
- 新增可測試的連線交叉計數與畫布 ribbon 產生器；100 節點測試同時驗證無節點重疊、四向分布、階層向外及零嚴格交叉。
- 樹狀連線保留共用主幹與分段剝離，新增由深褐主幹到節點色的漸層、枝條外輪廓、虛線樹皮高光與分叉木節，讓主幹、粗枝與末梢更容易辨識。
- 智慧整理後自動適合畫面加入 40px 下移偏置，避開上方模式列、搜尋與提示遮住最上層節點。

### 驗證與發布準備

- `npm run lint` 與心智圖純函式測試通過；18 項心智圖測試涵蓋 100 節點、零交叉、卡片邊界錨點與既有樹狀主幹契約。
- 桌面 1280×720 實際完成「心智圖 → 智慧整理 → 樹狀檢視」；心智圖以 60% 顯示完整四向分布，7 條連線與 8 個節點清楚分離，復原入口可用。
- 樹狀畫面確認 2 段共用主幹、7 段枝條、9 組木質漸層及 9 條樹皮紋理均已渲染，主控台無相關警告或錯誤。

## 2026-07-25｜v0.18.1 AI 擴寫草稿連續多選

### 問題與決策

- 單筆加入 AI 擴寫草稿後，原流程會把目前焦點切換到新建立的子節點，使原節點的生成結果立刻離開畫面；使用者因此無法接著選擇同一組中的第二或第三項。
- 單筆加入的意圖是「採用一項建議」，不是離開目前思考主題。焦點與生成來源應維持原節點，直到使用者主動選擇其他節點或換一組草稿。

### 實作

- `addAiSuggestion` 建立子節點後保留原父節點的 `selectedId`，讓生成草稿、摘要與剩餘數量持續顯示。
- 草稿卡片依目前節點的既有子節點標題判定採用狀態；已加入項目顯示勾選與「已加入目前節點」，並停用重複加入。
- 其餘草稿保持可選，「全部加入圖中」只計算尚未採用的項目，支援逐項與整組操作交錯使用。

### 驗證與發布準備

- 真實 AI 針對「訪談三位使用者」生成 6 項草稿，連續加入前兩項後焦點仍為原節點；兩張卡片均顯示完成，剩餘數量依序由 6 降為 5、4。
- 兩個採用項目都已出現在畫布，其他 4 項仍可繼續加入；瀏覽器主控台沒有警告或錯誤。
- 正式建置、`git diff --check` 與 50 項自動測試全數通過。

## 2026-07-25｜v0.19.0 畫面置中、階層布局與分支收合

### 問題與決策

- 舊版「適合畫面」以固定 `1080×650` 偏移估算，未真正扣除命令列、縮放控制與手機 AI 面板；不對稱心智圖因此容易完整顯示卻讓中心主題偏離可用畫布中央。
- 智慧整理雖已採環狀扇區，但所有卡片尺寸相同，資訊層級不夠明顯；碰撞、連線與匯出也需要共享同一套尺寸來源，避免視覺與計算結果不一致。
- 分支收合屬個人閱讀偏好，不應寫入 D1、影響共享者或污染復原紀錄，因此採用依地圖 ID 隔離的版本化裝置端檢視狀態。

### 實作

- `app/lib/mindmap.ts` 新增一次性深度索引、三層節點尺寸、可見節點過濾與 `calculateFitTransform` 純函式。適合畫面以中心主題四周的不對稱範圍計算縮放，並把實際倍率納入平移；總覽最低支援 10%。
- 全圖智慧整理依子樹葉節點數分配扇區，父節點位於後代角度區間中央，各層半徑依實際卡片尺寸與密度推進；指定分支整理保持分支根和外部節點不動。
- 心智圖與樹狀檢視依深度套用 `204×94`、`180×82`、`152×70` 三種卡片；連線邊界、碰撞、樹狀布局與 PNG／PDF 匯出共用相同規格。
- 選取具有子節點的節點時顯示獨立「收合 N／展開 N」控制與 `aria-expanded`。收合隱藏完整後代及連線；若焦點位於被隱藏子樹，會回到父節點。
- `app/lib/viewState.ts` 新增 `MapViewState { version: 1, collapsedIds }`，依本機或雲端地圖 ID 保存。資料解析會去重、限制數量並忽略損壞格式；節點刪除、重設範例與不存在 ID 會自動清理。

### 驗證與發布準備

- 55 項自動測試涵蓋不對稱置中、100 節點避讓、深度尺寸、全圖／分支布局錨定、多層收合及裝置端檢視狀態隔離與損壞資料。
- 桌面 1280×720 實際驗證中心主題等於安全畫布中心、三種卡片尺寸與完整邊界；收合後節點與連線數正確減少，重新整理保留狀態，展開後完整恢復。
- 手機 390×844 以 18% 完整顯示心智圖，中心主題位於可用畫布中央；樹狀模式完整顯示樹冠，樹根靠下且主幹水平置中。兩種尺寸主控台均無警告或錯誤。
- `npm run lint`、正式建置、`git diff --check` 與完整測試已通過；GitHub 與 Sites 待發布版本會使用同一個 commit，正式站部署仍等待使用者明確確認。

## 2026-07-25｜v0.19.1 AI 擴寫草稿捲動修正

### 問題與決策

- 真實 AI 通常產生 5–6 組擴寫草稿，但右側面板的 flex 內容區缺少明確最小高度與穩定捲動槽；第五組容易落在畫面底部之外，使用者不易發現或點選。
- 保留整個 AI 內容區單一捲動，不增加卡片內部或第二層捲動，避免形成捲動陷阱。

### 實作

- AI 面板加入 `min-height: 0` 與裁切邊界，內容區固定使用垂直捲動、穩定 scrollbar gutter 及清楚的捲動軌道與滑塊。
- 增加內容底部留白與建議卡片 scroll margin，讓最後一組能完整停在面板可視範圍，手機 bottom sheet 同步套用。
- 回歸測試檢查面板收縮、垂直捲動、穩定 gutter 與可見捲動軸樣式，避免後續版面調整再次移除。

### 驗證與發布

- 真實 AI 產生 6 組草稿；桌面 1280×720 的內容高度 568px、可捲內容 1080px，最後一組完整可見並成功加入。
- 手機 390×844 的內容高度 541px、可捲內容 1084px，第五組完整位於面板內並成功加入；兩種尺寸主控台均無警告或錯誤。
- `npm run lint`、正式建置、`git diff --check` 與 55 項自動測試全數通過。

## 2026-07-25｜v0.20.0 手勢縮放

### 問題與決策

- 縮放先前只能透過左下角的 ±10% 按鈕，滑鼠滾輪與觸控板在畫布上不作用，觸控裝置也無法雙指縮放；使用者必須反覆按鈕才能調整大型地圖。
- 決定把錨點運算抽成純函式 `calculateAnchoredZoom`，讓「游標下的地圖座標保持不動」這個不變式可用單元測試證明，UI 只負責取得矩形與手勢資料。
- 手勢縮放採連續倍率而非 10% 級距；按鈕維持 10% 級距以保留可預期的鍵盤與點擊行為。縮放值取到小數一位，避免高頻更新造成整數跳動。

### 實作

- [app/lib/mindmap.ts](./app/lib/mindmap.ts) 新增 `calculateAnchoredZoom`，依縮放比例調整總位移，使錨點在縮放前後對應同一地圖座標。
- [app/MindMapStudio.tsx](./app/MindMapStudio.tsx) 新增滾輪處理：以 `deltaMode` 正規化行／頁單位，夾在 ±240px，並以指數曲線換算縮放倍率；命令列、縮放控制與節點編輯器內的滾動不攔截。
- 觸控雙指以 capture 階段的 pointer 事件追蹤；第二指落下時中止進行中的節點拖曳或平移，若該拖曳已移動則先寫入一筆歷史，避免手勢吃掉復原紀錄。
- 雙指同時支援縮放與平移：以雙指中心位移作為 `anchorShift` 疊加在錨點縮放結果上。
- 縮放與平移狀態改以 `zoomRef`／`stageOffsetRef`／`stagePanRef` 讀取最新值，並透過 `queueViewportUpdate` 做動畫幀合併，避免高頻事件觸發多餘 render。

### 驗證與發布

- 滑鼠滾輪縮放從 100% 調整至 127% 時，游標錨點位移小於 0.001 像素；單元測試以反算地圖向量的方式斷言錨點完全不動。
- `npm run lint`、正式建置、`git diff --check` 與 56 項自動測試全數通過；主控台無警告或錯誤。

## 2026-07-26｜v0.21.0 觸控與無障礙收斂（P0-14）

### 盤點

先對照 P0-14 的四項驗收條件逐一檢查現況，而不是直接改樣式：

- 觸控目標：節點的編輯／整理／移植／新增按鈕在桌面是 24×24px，手機是 28×28px、細節層級只有 24×24px；大綱收合 28×28px、AI 送出鈕 30×30px、收件匣關閉鈕 34×34px 等多處低於 44px。
- 焦點：焦點外框使用 `rgba(237,118,95,.38)`，與紙張底色合成後對比只有 1.42:1，遠低於 3:1；節點選取外框 42% 透明度也只有 1.48:1。`a`、`select`、`summary` 完全沒有焦點樣式；畫布節點是不可聚焦的 `<article>`，鍵盤使用者只能從大綱操作。
- 200% 縮放：`.app-shell` 的 `min-height:680px`（手機 760px）超過 1280×800 螢幕在 200% 縮放後的 400px 可視高度，面板會被推出畫面。
- 拖曳替代：大綱排序有上移／下移、層級變更有縮排／凸排與移植對話框，但「移動節點位置」沒有任何非拖曳路徑。

### 決策

- 觸控尺寸用 `@media (pointer:coarse)` 統一保證，而不是把桌面的密集版面也一起放大；桌面維持既有視覺密度。
- 4 個 44px 按鈕（含間距約 202px）放不進 152px 的細節卡片，因此觸控時節點操作列改為浮出於**選取**卡片下方，並只對選取節點顯示。這同時解決了卡片內部的擁擠，且不需要新的動作面板 UI。
- 方向鍵移動節點沿用拖曳的歷史語意：一連串按壓只建立一筆復原紀錄，樹狀模式只改檢視偏移量、不進歷史。
- 方向鍵只在焦點位於 `.mind-node` 或 `.canvas` 時接管，其他位置維持原生捲動行為。

### 實作

- [app/lib/mindmap.ts](./app/lib/mindmap.ts) 新增 `nudgeVectorForKey` 與 `moveNodeBy`，把「哪個鍵移動多少」與「移動單一節點」兩件事抽成可測純函式。
- [app/MindMapStudio.tsx](./app/MindMapStudio.tsx) 的節點加上 `tabIndex={0}` 與 `onFocus` 選取；`beginNodeDrag` 因為 `preventDefault` 會抑制隱式聚焦，改為明確呼叫 `focus({ preventScroll: true })`。
- 新增 `keepNodeInView`：聚焦或鍵盤移動後，若節點落在命令列、縮放控制或畫布邊界之外，就用最小位移平移畫布；量測時間排在 `.24s` 位置轉場結束之後（`NUDGE_SETTLE_MS`），避免讀到動畫中途的座標。
- [app/globals.css](./app/globals.css) 統一焦點指示為 `--focus-ring:#a5391f`（對紙張 5.92:1、對暖白 6.40:1），深色底控制項改用 `--sun`（對 ink 11.80:1）；節點選取外框改為 82% 不透明度（4.22:1）。
- 新增 `@media (pointer:coarse)` 區塊涵蓋工具列、命令列、收件匣、大綱、AI 面板、個人地圖與各對話框；節點操作列改為 `left:50%` 置中浮出、按鈕 44×44px。
- 外框最小高度由 680px／760px 降為 480px，`.workspace` 由 604px 降為 404px，並改用 `dvh`；桌面工具列加上 `overflow-y:auto`。

### 驗證

- 純函式測試涵蓋方向鍵對應（含非方向鍵必須回傳 null，以免吃掉頁面捲動）、Shift 大步距，以及 `moveNodeBy` 不動後代、不變更輸入、無位移時回傳原陣列。
- 迴歸測試把驗收條件寫成斷言：`(pointer:coarse)` 區塊內所有尺寸宣告必須 ≥44px、`.app-shell` 系列的 `min-height` 必須 ≤480px、低對比焦點環不得回歸、方向鍵只在地圖內接管、一次連續按壓只建立一筆 checkpoint。
- 對比度以 WCAG 相對亮度公式實算，含 alpha 合成後的實際顏色，而非直接取色票值：舊焦點環 1.42:1、舊選取外框 1.48:1；新值分別為 5.92:1（暖白底 6.40:1）與 4.22:1，深色底暖色環對 ink 為 11.80:1。
- `npm run lint`、正式建置、`git diff --check` 與 58 項自動測試全數通過。
- 尚未取得實體觸控裝置與螢幕閱讀器的人工驗收，本次只以自動化斷言與實算對比度為證據。

## 2026-07-26｜v0.22.0 大型心智圖效能基準（P0-15）

### 決策

- 固定使用 100、300、500 節點三種規模，避免只驗證目前 PRD 的 100 節點最低門檻，卻無法看見成長趨勢。
- 高頻互動資料路徑（拖曳、搜尋、輸入）p95 預算為 16ms；畫面衍生資料為 50ms；非互動樹狀布局為 300ms。
- 基準腳本使用確定性節點與暖機後的 80 次樣本；超出預算時回傳非零結束碼，讓後續 CI 可以直接採用。
- 本機畫面驗證入口只在 development 環境存在，production 直接 404，避免把測試資料入口公開給正式使用者。

### 實作

- 新增 `scripts/performance-benchmark.mjs` 與 `npm run benchmark`，量測拖曳狀態更新、正規化搜尋、文字輸入狀態、深度／子節點／連線衍生資料與樹狀布局。
- `buildChildrenByParent` 將父子結構整理為共用索引；大綱階層、子節點數量、父節點與同層節點不再在每一列重複掃描完整陣列。
- `buildNodeSearchIndex` 與 `findMatchingNodeIds` 將搜尋正規化集中為可測純函式；React 端以 `useDeferredValue` 讓搜尋醒目標示保持非緊急更新。
- 新增 `/performance-benchmark?nodes=100|300|500` 本機開發入口，直接使用完整 `MindMapStudio` 渲染大型資料。

### 基準結果

| 節點 | 拖曳狀態 p95 | 搜尋 p95 | 輸入 p95 | 畫面衍生資料 p95 | 樹狀布局 p95 |
|---:|---:|---:|---:|---:|---:|
| 100 | 0.003ms | 0.023ms | 0.002ms | 0.428ms | 0.073ms |
| 300 | 0.004ms | 0.012ms | 0.003ms | 1.011ms | 0.112ms |
| 500 | 0.013ms | 0.036ms | 0.004ms | 1.624ms | 0.371ms |

所有結果均低於 16／50／300ms 預算；60 項自動測試通過。量測數字是同一台開發機上的相對迴歸基準，不代表所有裝置的絕對畫面幀率。

## 2026-07-26｜v0.23.0 個人生產力重用、匯入與偏好（P1-07～09）

### 決策與實作

- 分支複製與三個內建範本共用純資料操作；整棵子樹重建 ID 與父節點後再自動布局，一次只建立一筆 checkpoint。
- JSON 接受原始節點陣列或 `{version,title,nodes}` 封套；Markdown 以標題層級建立父子結構，也能正確略過靈感樹既有匯出檔的文件標題與時間。
- 匯入先解析成預覽，使用者確認後才取代地圖；語法與階層錯誤回報行號，節點結構錯誤回報節點序號。
- 使用者偏好採版本化 `localStorage`，保存預設檢視、AI 面板、縮放與減少動態；不寫入共享地圖，避免影響其他人的檢視。

### 驗證

- 65 項自動測試通過，涵蓋 P1-07～09 的純資料契約與既有功能迴歸。
- 使用隔離暫存副本完成 production build；工作區建置器的偵錯埠限制不影響正式產物。

### 交付狀態

- Git 提交：`2fbdd66 Add branch reuse imports and preferences`。
- 遠端分支：`origin/agent/p0-15-performance-benchmark`，本機與遠端已同步。
- 文件版本：產品 `v0.23.0`；PRD 的 P1-07～09 與 Roadmap 的 M3 已標記完成。
- 正式網站尚未部署；GitHub 推送不等同 Sites 正式發布。

### M3 收尾

- **完成**：P1-06～11 的裝置端個人生產力能力。
- **延後研究**：P1-03 AI 進階上下文與 P1-04 AI 對話歷史，待證實不增加主要流程負擔後再啟動。
- **下一里程碑**：M4 可控分享，從 P2-01 伺服器端分享權限開始，再承接到期／撤銷、留言、版本與活動紀錄。

## 2026-07-26｜v0.24.0 節點 AI Assistant（P1-12）

### 產品決策

- 下一階段優先投資「AI 生產力」，不再以增加一般編輯控制作為主要方向。
- AI Assistant 掛在節點操作上：桌面以右鍵快速開啟，節點「✦」按鈕同時提供觸控與鍵盤替代路徑。
- AI 不直接改圖。分析型指令先產生可選建議，簡化／翻譯／摘要先產生解讀預覽；使用者採用後才建立節點或寫回說明。

### 實作

- `AI_ASSISTANT_COMMANDS` 集中定義八種指令的標籤、圖示、既有安全模式、提示詞與結果類型。
- 展開、風險、SWOT、OKR 與 Brainstorm 對應結構化建議；簡化內容、翻譯成英文與摘要對應結構化解讀。
- 畫布與大綱節點皆支援右鍵；選單會限制在視窗內，Escape、失焦或點擊外部可關閉。
- 結果沿用既有節點 ID 白名單、回應長度驗證、AI 面板與復原機制，沒有新增平行 API。

### 驗證與限制

- 66 項自動測試通過，新增八種指令順序、模式映射、提示詞與結果類型測試。
- production build 成功；`git diff --check` 通過。
- 本機 Vinext production start 仍受 `cloudflare:` ESM 執行環境限制，未完成瀏覽器畫面與真實 AI 金鑰端到端驗收。

## 2026-07-26｜v0.25.0 精選範本商城（P1-13）

### 產品決策

- 先交付可用的精選商城，不把尚未存在的第三方投稿、交易或審核能力包裝成已完成。
- P1-13 解決「從成熟結構開始」；P2-08 保留給真正的公開投稿、發布者管理、撤下與來源不可變契約。

### 實作

- 範本由 3 個擴充至 11 個，涵蓋 SMART、決策、每週回顧、OKR、軟體專案、課程設計、研究、行銷、SCRUM、AI Prompt 與人生規劃。
- 資料模型新增分類、標籤、作者、精選狀態與圖示；`filterBranchTemplates` 集中處理關鍵字與分類篩選。
- 商城提供搜尋、9 個分類、範本卡片、無結果狀態與右側結構預覽；手機保留單欄卡片與直接套用。
- 套用仍使用既有 ID 重建、自動布局與單一 checkpoint，不另建第二套範本執行邏輯。

### 驗證與限制

- 67 項自動測試通過，production build 與 `git diff --check` 通過。
- Git 提交 `cb5e916 Add AI assistant and template marketplace` 已推送至 `origin/agent/p0-15-performance-benchmark`。
- Sites 正式部署成功，公開網址：https://inspiration-tree-ai-mindmap.go38.chatgpt.site
- 目前目錄隨版本內建；尚無投稿、評分、收藏、遠端更新或付費交易。
- 本機 Cloudflare 執行環境限制仍阻擋互動式瀏覽器驗收。

## 2026-07-26｜v0.26.0 AI 自動產圖（P1-14）

### 產品決策

- 自動產圖採「生成草稿 → 預覽 → 使用者確認」三步，不允許 AI 回應直接覆蓋目前地圖。
- 第一版提供精簡、標準、深入三種節點預算，分別上限 12、24、40，避免固定一種規模無法兼顧速度與深度。
- 確認建立會取代目前地圖並建立一筆 checkpoint；標題、布局、收合與樹狀偏移同步重設。

### 實作

- 新增 `/api/generate-map`，使用 Responses API 嚴格 JSON Schema 產生扁平階層草稿。
- `aiMap.ts` 集中處理請求限制、提示詞、草稿驗證與 `NodeItem` 物化；要求唯一中心、父節點必須先存在、key 唯一且色調合法。
- 產圖對話框包含主題、詳細程度、載入／錯誤狀態、標題與摘要，以及可捲動的完整大綱預覽。
- 工具列新增「✣」入口；AI 未設定、配額、服務失敗、逾時與解析錯誤皆不改動目前地圖。

### 驗證與限制

- 71 項自動測試、production build 與 `git diff --check` 通過。
- 真實生成仍需要 Sites 正式環境的 `OPENAI_API_KEY`；本機未完成含真實模型的瀏覽器端到端驗收。
- 第一版只建立新圖，不提供「插入目前分支」或多輪修改草稿。

## 2026-07-26｜v0.27.0 PDF／網站／影音知識匯入（P1-15）

### 產品決策

- 三種來源共用「選擇來源 → AI 整理 → 草稿預覽 → 確認建立」，未確認前不修改目前地圖。
- PDF 直接以 Responses API 的 `input_file` 交給模型；網站由伺服器擷取公開 HTML 正文；影音第一版以使用者提供的合法字幕或逐字稿為準。
- 不宣稱能繞過登入、DRM 或平台限制取得字幕；私人或需登入內容應由使用者自行匯出後上傳。

### 實作

- 新增 `/api/knowledge-import`，三種來源最後共用 AI Map JSON Schema 與階層驗證。
- PDF 上限 8MB；網站限公開 HTTPS、HTML、1.5MB 與 12 秒，並拒絕私人 IPv4、localhost、`.local` 與重新導向。
- 逐字稿支援貼上或 TXT、Markdown、SRT、VTT，上限 80,000 字元；可附影音網址作來源標示。
- 工具列新增「◫」知識匯入入口，包含來源切換、載入／錯誤狀態、完整大綱預覽與可復原套用。

### 驗證與限制

- 74 項自動測試與 `git diff --check` 通過；production build 在隔離暫存副本驗證。
- JavaScript 動態網站、付費牆、登入頁面與重新導向網址不支援；請改貼正文或最終公開網址。
- PDF 與來源文字會傳送至設定的 OpenAI API 進行整理；正式環境需配置有效 `OPENAI_API_KEY`。

## 2026-07-27｜v0.28.0 分享權限（P2-01）

### 產品決策

- 分享預設關閉；本機地圖先以登入身分建立私人雲端地圖，再由擁有者主動開啟分享。
- 第一版定義唯讀、可留言、可編輯三種角色；留言角色先具備資料與介面契約，實際討論功能留給 P2-03。
- 分享 token 是不可猜測的能力網址，但所有讀寫仍必須經伺服器查詢有效狀態與角色，不能只靠前端隱藏按鈕。

### 實作

- 新增 `share_links` D1 表與 migration，每張地圖維持一個分享設定，可啟用、停用、變更角色或重建 token。
- 新增擁有者限定的 `/api/maps/:id/share`，以及公開 `/s/:token` 訪客頁面。
- 地圖更新 API 接受有效的編輯 token；唯讀與留言 token 回傳 403，編輯連結沿用版本衝突保護。
- 分享管理視窗提供狀態切換、角色說明、網址複製與重建；訪客頁顯示目前角色並收斂編輯控制。

### 驗證與限制

- 76 項自動測試、`git diff --check` 與 production build 通過。
- 「可留言」目前等同唯讀並顯示即將開放說明；節點留言、回覆與解決狀態屬 P2-03。
- 到期日與具名邀請尚未支援，分別留給 P2-02 與後續協作權限擴充。

## 2026-08-07｜v0.28.1 左側工具列資訊架構收斂（P0-22）

### 問題

- 左側工具列隨 AI 自動產圖、知識匯入、範本、偏好與分享能力持續增加，累積至 14 個大型按鈕與多個分隔線。
- 在 480px 短視窗、瀏覽器放大或手機畫面下，使用者必須捲動工具列才能找到大綱、偏好與重設等底部工具，且低頻功能與核心編輯操作具有相同視覺權重。

### 產品決策

- 主列只常駐新增、AI 自動產圖、靈感收件匣、復原／重做、大綱與更多工具，讓核心工作流保持一眼可見。
- 移除、複製、範本、內容匯入、知識匯入、偏好與重設不刪除，而是以漸進揭露方式集中到「更多工具」面板。
- 主列分成上方創作入口與下方檢視／歷史入口；復原與重做合併為一個雙按鈕控制，降低垂直占用。

### 實作

- 新增具 `aria-haspopup`、`aria-expanded` 與 `role="menu"` 的更多工具面板；點擊背景或完成任一操作後自動關閉。
- 面板以固定定位顯示在工具列旁，不受工具列自身捲動與裁切影響；手機使用左右安全間距與自適應寬度。
- 高頻按鈕由 52px 收斂至 46px，新增操作維持深色主要層級；復原／重做共享 46px 寬的分段控制。

### 驗證與發布狀態

- 76 項自動測試全數通過，涵蓋節點、AI、知識匯入、分享權限、保存、效能與伺服器渲染。
- `git diff --check` 通過；本機 production build 在執行環境中長時間無輸出，未在本次觀察窗口內完成，因此不記為成功。
- 變更已推送至 Sites 來源並保存為第 36 版；網站目前為共享／公開狀態，正式部署等待使用者明確核准。

## 2026-08-07｜v0.28.2 Safari 更多工具面板修正

- Safari 對可捲動元素內的 `position: fixed` 後代可能套用裁切，造成狀態已展開但面板不可見。
- 將更多工具面板與透明關閉背景移到 `.toolrail` 之外，讓固定定位相對工作區呈現，不再受工具列 `overflow-y:auto` 影響。
- 新增結構回歸測試，保證面板來源位置永遠晚於工具列關閉標籤；伺服器渲染與原始碼測試通過。
- 本機 production build 再次於執行環境長時間無輸出，未記為成功；正式 Sites 部署使用推送後的精確來源版本進行遠端建置。

## 2026-08-07｜v0.29.0 工具列三模式循環

- 三條線按鈕原本只在心智圖與大綱間切換，樹狀檢視只能透過畫布上方的模式控制進入。
- 新增 `cycleViewMode`，依 `canvas → tree → outline → canvas` 循環並沿用既有 `selectViewMode`，因此樹狀模式仍會執行適合畫面與樹冠定位。
- 按鈕提示與無障礙名稱從目前模式直接衍生下一個模式，沒有新增同步狀態或 effect。
- 伺服器渲染與原始碼回歸測試通過。

## 目前已知限制

- 尚未支援多人共同即時編輯。
- 真實 AI 需要在 Sites 正式環境設定 `OPENAI_API_KEY`；未設定時使用離線示範建議。
- P0-14 的觸控尺寸、焦點對比與 200% 縮放高度目前以自動化斷言把關，尚未在實體觸控裝置與螢幕閱讀器上完成人工驗收。
- 手機版大綱仍隱藏編輯、移植與新增子節點按鈕；這些操作要回到心智圖檢視的節點操作列完成。
- 畫布節點全部進入 Tab 順序，大型地圖的鍵盤巡覽會偏長，尚未提供跳過畫布的捷徑。
- 樹狀手動偏移目前只保留於當次編輯工作階段；重新整理頁面會回到自動樹冠，不會同步到雲端。
- 靈感收件匣目前採每張地圖的裝置端保存，尚未跨裝置同步；將種子種下後的正式節點仍依原有地圖保存方式同步。
- 共享地圖為儲存後同步，尚非多人即時協作。

## 建議後續工作

1. 開發 P2-01 分享權限與 P2-02 到期／撤銷。
2. 在實體觸控裝置與螢幕閱讀器上完成 P0-14 的人工驗收，並補上手機大綱缺少的節點操作。
3. 規劃 P2-08 公開範本庫，沿用 P1-07 的範本資料契約。

## GitHub 與版本狀態

- GitHub：https://github.com/go38/inspiration-tree-ai-mindmap
- 版本策略：Semantic Versioning，規範見 [VERSIONING.md](./VERSIONING.md)。
- 發佈紀錄：見 [CHANGELOG.md](./CHANGELOG.md)。
